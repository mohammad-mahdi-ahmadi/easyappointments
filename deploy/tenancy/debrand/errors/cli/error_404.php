<?php
/**
 * CLI error view (ADDITIVE). Carries no marketing, but $config['error_views_path'] points CI at this
 * tree as a whole, so upstream's template is rendered from here rather than copied.
 */
include APPPATH . 'views/errors/cli/error_404.php';
